name: echo-issue.yml
on: [push, workflow_dispatch]

jobs:
  issues:
    runs-on: ubuntu-latest
    steps:
      - name: Echo repo issue
        run: echo "${{ toJSON(issue) }}"
      - name: Echo Github context
        run: echo "${{ toJSON(github) }}"